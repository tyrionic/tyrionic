# Classes

[`class.ty`](class.ty) defines a `Greeter` class, initializes object state, and
invokes a bound method.

```sh
./build/tyrionic --build ./examples/class.ty --out ./build/class
./build/class
```

