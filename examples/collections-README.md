# Collections

[`collections.ty`](collections.ty) demonstrates lists, tuples, dictionaries,
sets, indexing, slicing, mutation methods, `keys()`, and `items()`.

```sh
./build/tyrionic --build ./examples/collections.ty --out ./build/collections
./build/collections
```

