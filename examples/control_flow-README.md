# Control Flow

[`control_flow.ty`](control_flow.ty) covers functions, `if`/`elif`/`else`,
iteration, `range`, `enumerate`, and `while`.

```sh
./build/tyrionic --build ./examples/control_flow.ty --out ./build/control_flow
./build/control_flow
```

