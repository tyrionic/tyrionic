# Assignments

[`direct_native_assignments.ty`](direct_native_assignments.ty) demonstrates
dictionary and list mutation, augmented assignment, negative indexes, and
starred unpacking.

```sh
./build/tyrionic --build ./examples/direct_native_assignments.ty --out ./build/assignments
./build/assignments
```

