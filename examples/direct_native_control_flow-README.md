# Native Control Flow

[`direct_native_control_flow.ty`](direct_native_control_flow.ty) demonstrates
nested branching inside a `while` loop and mutable numeric state.

```sh
./build/tyrionic --build ./examples/direct_native_control_flow.ty --out ./build/native_control_flow
./build/native_control_flow
```

