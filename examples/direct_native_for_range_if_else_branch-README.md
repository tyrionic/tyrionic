# Range Branching

[`direct_native_for_range_if_else_branch.ty`](direct_native_for_range_if_else_branch.ty)
demonstrates an `if`/`else` branch inside a `for` loop over `range`.

```sh
./build/tyrionic --build ./examples/direct_native_for_range_if_else_branch.ty --out ./build/range_branch
./build/range_branch
```

