# Functions And Data

[`direct_native_functions_data.ty`](direct_native_functions_data.ty) covers
default and keyword arguments, loops, lists, dictionaries, indexing, and scalar
conversion builtins.

```sh
./build/tyrionic --build ./examples/direct_native_functions_data.ty --out ./build/functions_data
./build/functions_data
```

