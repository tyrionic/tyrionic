# Generators

[`direct_native_generators.ty`](direct_native_generators.ty) defines generator
functions with `yield`, consumes them in comprehensions and loops, and uses
default and keyword arguments.

```sh
./build/tyrionic --build ./examples/direct_native_generators.ty --out ./build/generators
./build/generators
```

