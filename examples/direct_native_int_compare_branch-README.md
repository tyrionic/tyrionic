# Integer Comparison

[`direct_native_int_compare_branch.ty`](direct_native_int_compare_branch.ty)
uses an arithmetic expression as part of an integer comparison and conditional
branch.

```sh
./build/tyrionic --build ./examples/direct_native_int_compare_branch.ty --out ./build/int_compare
./build/int_compare
```

