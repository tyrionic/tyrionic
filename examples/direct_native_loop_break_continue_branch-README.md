# Break And Continue

[`direct_native_loop_break_continue_branch.ty`](direct_native_loop_break_continue_branch.ty)
demonstrates `break` and `continue` in a stateful `while` loop.

```sh
./build/tyrionic --build ./examples/direct_native_loop_break_continue_branch.ty --out ./build/loop_control
./build/loop_control
```

