# Direct native process primitives

This focused conformance application exercises both process APIs and verifies
success, non-zero and missing commands, stdout/stderr capture, exact argument
boundaries, large output, signal normalization, and repeated execution.
