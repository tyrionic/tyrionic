# File And Function Demo

[`tail_demo.ty`](tail_demo.ty) demonstrates file seeking and reading, string
splitting, loops, default and keyword arguments, and `time()`. It creates
`examples_data.log` in the current directory.

```sh
./build/tyrionic --build ./examples/tail_demo.ty --out ./build/tail_demo
./build/tail_demo
```

